<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.splitlink.mapper.ExpenseMapper">

    <!-- 지출 내역 등록 -->
    <insert id="insertExpense"
            parameterType="com.splitlink.entity.Expense"
            useGeneratedKeys="true"
            keyProperty="expenseId"
            keyColumn="expense_id">
        INSERT INTO expenses(
        room_id,
        payer_id,
        title,
        amount,
        currency,
        fx_rate,
        spent_at
        ) VALUES (
        #{roomId},
        #{payerId},
        #{title},
        #{amount},
        #{currency},
        #{fxRate},
        #{spentAt}
        )
    </insert>

    <!-- 지출 분할 참여자 Bulk INSERT -->
    <insert id="insertExpenseShares">
        INSERT INTO expense_shares (
        expense_id,
        member_id,
        amount
        ) VALUES
        <foreach collection="shares" item="share" separator=",">
            (
            #{share.expenseId},
            #{share.memberId},
            #{share.amount}
            )
        </foreach>
    </insert>

    <!-- 방 전체 총 지출 합계 -->
    <select id="findTotalExpenseAmountByRoomId" resultType="java.math.BigDecimal">
        SELECT IFNULL(SUM(amount), 0)
        FROM expenses
        WHERE room_id = #{roomId}
    </select>

    <!-- 지출 내역 목록 조회 -->
    <select id="findExpenseItems" resultType="com.splitlink.dto.response.ExpenseListResponse$ExpenseItemResponse">
        SELECT
            e.expense_id AS expenseId,
            e.title AS title,
            e.amount AS amount,
            m.name as payerName,
            CASE
                WHEN e.payer_id = #{memberId}
                THEN true
                ELSE false
            END AS isMyPayment,
            (SELECT COUNT(*)
             FROM expense_shares es
             WHERE es.expense_id = e.expense_id
            ) AS targetMemberCount
        FROM expenses e
        INNER JOIN members m on e.payer_id = m.member_id
        WHERE e.room_id = #{roomId}
        ORDER BY e.expense_id DESC
    </select>

    <!-- 지출 기본 상세 정보 및 결제자 계좌 조회 -->
    <select id="findExpenseDetailById" resultType="com.splitlink.dto.response.ExpenseDetailResponse">
        SELECT
            e.expense_id AS expenseId,
            e.title,
            e.amount,
            e.currency,
            e.fx_rate AS fxRate,
            e.spent_at AS spentAt,
            e.payer_id AS payerId,
            m.name AS payerName,
            m.bank_name AS bankName,
            m.account_number AS accountNumber,
            CASE WHEN e.payer_id = #{memberId} THEN TRUE ELSE FALSE END AS isMyPayment
        FROM expenses e
        JOIN members m ON e.payer_id = m.member_id
        WHERE e.expense_id = #{expenseId}
            AND e.room_id = #{roomId}
    </select>

    <!-- 해당 지출의 참여자별 최종 부담금 목록 조회 -->
    <select id="findExpenseSharesByExpenseId" resultType="com.splitlink.dto.response.ExpenseDetailResponse$TargetMemberDetail">
        SELECT
            es.member_id AS memberId,
            m.name,
            es.amount AS shareAmount,
            CASE WHEN es.member_id = #{memberId} THEN TRUE ELSE FALSE END AS isSelf
        FROM expense_shares es
        JOIN members m ON es.member_id = m.member_id
        WHERE es.expense_id = #{expenseId}
          AND m.room_id = #{roomId}
        ORDER BY es.member_id ASC
    </select>

    <!-- 지출 수정 폼 기본 데이터 조회 -->
    <select id="findExpenseUpdateFormById" resultType="com.splitlink.dto.response.ExpenseUpdateFormResponse">
        SELECT
            expense_id AS expenseId,
            title,
            amount,
            currency,
            spent_at AS spentAt,
            payer_id AS payerId
        FROM expenses
        WHERE expense_id = #{expenseId}
          AND room_id = #{roomId}
    </select>

    <!-- 해당 지출에 참여한 멤버 PK 목록 조회 -->
    <select id="findTargetMemberIdsByExpenseId" resultType="java.lang.Long">
        SELECT member_id AS memberId
        FROM expense_shares
        WHERE expense_id = #{expenseId}
    </select>

    <!-- 멤버별 (결제해 준 금액 - 부담해야 할 금액) 순 상계 금액 계산 -->
    <select id="findNetBalancesByRoomId" resultType="com.splitlink.dto.MemberNetBalanceDto">
        SELECT
            m.member_id AS memberId,
            (
            IFNULL(p.total_paid, 0) - IFNULL(s.total_shared, 0)
            ) AS netBalance
        FROM members m
        -- [A] 멤버별 대신 내준 금액 (Paid)
        LEFT JOIN (
            SELECT payer_id, SUM(amount * fx_rate) AS total_paid
            FROM expenses
            WHERE room_id = #{roomId}
            GROUP BY payer_id
        ) p ON m.member_id = p.payer_id
        -- [B] 멤버별 부담해야 할 금액 (Shared)
        LEFT JOIN (
            SELECT es.member_id, SUM(es.amount * IFNULL(e.fx_rate, 1)) AS total_shared
            FROM expense_shares es
            INNER JOIN expenses e ON es.expense_id = e.expense_id
            WHERE e.room_id = #{roomId}
            GROUP BY es.member_id
        ) s ON m.member_id = s.member_id
        -- 해당 방 소속 멤버 전체 대상
        WHERE m.room_id = #{roomId}
    </select>

    <!-- 방 기준 지출 건수 조회 -->
    <select id="countExpensesByRoomId" resultType="int">
        SELECT COUNT(*)
        FROM expenses
        WHERE room_id = #{roomId}
    </select>

    <!-- 지출 메인 정보 수정 -->
    <update id="updateExpense">
        UPDATE expenses
        SET
            payer_id = #{payerId},
            title = #{title},
            amount = #{amount},
            spent_at = #{spentAt}
        WHERE expense_id = #{expenseId}
          AND room_id = #{roomId}
    </update>

    <!-- 지출 내역 삭제 -->
    <delete id="deleteExpenseById">
        DELETE FROM expenses
        WHERE expense_id = #{expenseId}
          AND room_id = #{roomId}
    </delete>

    <!-- 해당 지출의 기존 부담금 데이터 삭제 -->
    <delete id="deleteExpenseSharesByExpenseId">
        DELETE FROM expense_shares
        WHERE expense_id = #{expenseId}
    </delete>
</mapper>
